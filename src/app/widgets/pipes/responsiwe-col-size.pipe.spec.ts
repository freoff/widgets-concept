import { ResponsiweColSizePipe } from './responsiwe-col-size.pipe'

describe('ResponsiweColSizePipe', () => {
  it('create an instance', () => {
    const pipe = new ResponsiweColSizePipe()
    expect(pipe).toBeTruthy()
  })
})
