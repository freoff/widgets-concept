import { WidgetFactoryDirective } from './widget-factory.directive'

describe('WidgetFactoryDirective', () => {
  it('should create an instance', () => {
    const directive = new WidgetFactoryDirective()
    expect(directive).toBeTruthy()
  })
})
